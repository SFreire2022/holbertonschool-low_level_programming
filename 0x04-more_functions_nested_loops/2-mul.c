#include "main.h"

/**
 * mul - multiplies two integers.
 *
 * @a: argument number as an integer.
 * @b: argument number as an integer.
 * Return: a * b result
 */
int mul(int a, int b)
{
	return (a * b);
}
