#include "main.h"

/**
 * add - Return a + b result.
 *
 * @a: argument number as an integer.
 * @b: argument number as an integer.
 * Return: a + b result
 */
int add(int a, int b)
{
	return (a + b);
}
